<div class=container>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%# un truc à faire ici %>
    <%= f.association :movie %>
    <%= f.submit %>
  <% end %>
</div>
