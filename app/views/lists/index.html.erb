
  <h1>This are my Lists</h1>
  <ul>
  <% @lists.each do |list| %>
    <li>
    <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
  </ul>

<%= link_to 'Add a list', new_list_path, class: 'btn btn-primary m-2' %>
